<?xml version="1.0" encoding="utf-8"?>
<!-- Android 12+ took the splash screen over: it ignores windowBackground and
     draws the launcher icon on its own background instead. Without these two
     attributes the app opens on the system's near-white default — exactly the
     flash the dark theme exists to avoid. -->
<resources>
    <style name="LaunchTheme" parent="@android:style/Theme.Black.NoTitleBar">
        <item name="android:windowSplashScreenBackground">@color/splash_background</item>
        <item name="android:windowSplashScreenAnimatedIcon">@mipmap/ic_launcher</item>
        <!-- Still needed for the window behind the system splash. -->
        <item name="android:windowBackground">@drawable/launch_background</item>
    </style>
    <style name="NormalTheme" parent="@android:style/Theme.Black.NoTitleBar">
        <item name="android:windowBackground">?android:colorBackground</item>
    </style>
</resources>
